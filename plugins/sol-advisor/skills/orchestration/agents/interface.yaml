interface:
  display_name: "Sol Advisor Orchestration"
  short_description: "Default routing: Sol owns critical judgment; native V2 balances Luna, Terra, and Sol by role"
  default_prompt: "Route through $sol-advisor:orchestration; keep critical judgment in Sol and use the balanced native Luna/Terra/Sol route matrix."
compatibility:
  canonical_format: "codex-plugin-skill"
  adapter_targets:
    - "openai"
  activation:
    mode: "manual"
    paths: []
  execution:
    context: "inline"
    shell: "bash"
  trust:
    source_tier: "plugin"
    remote_inline_execution: "forbid"
    remote_metadata_policy: "allow-metadata-only"
  degradation:
    openai: "Native V2 is required by default; app-task Luna and legacy Terra/Sol companion roles are explicit compatibility lanes."
